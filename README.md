# SinglyLinkedLists
self-contained C# example demonstrating a simple Saga orchestrator for an order creation flow using the Saga pattern (orchestration style).This implementation uses LinkedList<T> to store the executed steps allowing cheap appends during forward execution and easy reverse-order compensation on failure.

